# PostgreSQL — hem Drizzle (apps/web) hem docker-compose bu değerleri kullanır.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_SSL=false
POSTGRES_DB=postgres
POSTGRES_USER=bilalgursen
POSTGRES_PASSWORD=change_me

# Not: Strapi (apps/cms) kendi .env dosyasını kullanır (DATABASE_* + secrets).
